package CollectionFramework;
//Collection Framework provides us a set of interfaces and classes that help in managing group of objects
public class Main {
    //collection frameworks have Iterable interface at the top, that is why we can use for each loop to iterate through them

    public static void main(String[] args) {
        
        //1.)The list interface is a part of the java.util package and is a sub interface of the collection interface.
        //We use list interface when we want to store an ordered collection of elements where the elements can be duplicate.
        //Key Features are Order Preservation, Index Based Access and Allows Duplicates
    }
}
