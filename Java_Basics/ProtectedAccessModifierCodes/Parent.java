package ProtectedAccessModifierCodes;
//Protected access modifier allows the methods,memebers or constructors to be accessed by classes within the same package or by the subclasses
public class Parent {
    
    protected void display() {
     System.out.println("I am a function from the Parent Class!");
    }
}
