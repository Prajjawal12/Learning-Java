package ThreadingAndConcurrency;

public class Main {
    //Programs  are executed within a CPU which is oftern referred as Brain of the computer.

    //Core is an individual unit within a CPU where processing is done i.e processing unit.
    //Modern day CPUs have multiple cores.



    //Program is a set of instructions
    //Process is an instance of a program in execution

    //Thread is the smallest unit of execution within a process.A process can have multiple threads,which share
    //the same resources but run independently
}
